<%= form_with(model: nominee) do |form| %>
  <% if nominee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nominee.errors.count, "error") %> prohibited this nominee from being saved:</h2>
      <ul>
        <% nominee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% else %>
    <td><%= form.text_field :weightage, placeholder: 'weightage'  %></td>
     <td><%= form.hidden_field :nominee_id, value: @nominee.nominee_id  %></td>
     <td><%= form.hidden_field :user_id, value: current_user.id  %></td>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
